import Controller from '@ember/controller';

export default Controller.extend({
  // value: 1,
  // value: this.get('model.polygon').get('id'),
   //filteredNotes: Ember.computed.filterBy('note', 'polyId', this.get('model.polygon').get('id')),
  // actions: {
    //  getMyId() {
      //	console.log(this.value)
     // }
  // } 
});
